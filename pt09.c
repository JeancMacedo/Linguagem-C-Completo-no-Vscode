#include <stdio.h>
int main(){

    // augmented assignmente operator = used to replace a statement where an operator
    //                                  takes a variable as one if its arguments
    //                                  and then assigns the result back to the sam variable
    //                                  x = x + 1
    //                                  x+=1

    int x = 10;
    
    // x = x + 2;
    // x+=2;

    // x = x - 3;
    // x-=3;

    // x = x * 4;
    // x*=4;

    // x = x / 5;
    // x/=5;

    // x = x % 2;
    // x%=2;

    return 0;
}