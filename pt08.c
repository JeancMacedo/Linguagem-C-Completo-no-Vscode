#include <stdio.h>

int main(){

    //artithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decerement

    int x = 5;
    int y = 2;

    //int z = x + y;
    //int z = x - y;
    //int z = x * y;
    //float z = x / (float) y;
    //int z = x % y;
    x++;
    y--;

    printf("%d",x);
    printf("\n%d",y);

    return 0;
}
